"""Context uAgent — looks up user's past documents, finds similar prior letters.

This agent is ALSO exposed as an MCP server (see mcp_server/server.py)
for the Cognition Augment the Agent challenge.
"""
# TODO: uagents.Agent with Chat Protocol
# Listens for ContextRequest, returns ContextBundle with related past docs
