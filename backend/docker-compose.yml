version: "3.8"
services:
  backend:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
      - "8001:8001"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
