"""Supabase server-side client — auth verification, photo storage."""
import os
# TODO: implement
# Functions:
#   verify_jwt(token: str) -> user_id
#   upload_photo(user_id: str, photo_bytes: bytes) -> photo_url
