"""Twilio — phone calls, SMS, Media Streams."""
import os
# TODO: implement
# Functions:
#   initiate_call(to_number: str, stream_url: str) -> call_sid
#   send_sms(to_number: str, body: str) -> message_sid
